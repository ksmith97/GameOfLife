{
  "name": "game-of-life",
  "version": "1.0.0",
  "description": "An implementation of the game of life",
  "main": "lifegame.js",
  "scripts": {
    "build": "mkdir -p dist && browserify ./lifegame.js > dist/bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "watchify *.js -o 'browserify *.js > dist/bundle.js'"
  },
  "author": "Kevin Smith",
  "license": "ISC",
  "devDependencies": {
    "browserify": "14.1.0",
    "http-server": "^0.9.0",
    "watchify": "^3.9.0"
  },
  "dependencies": {
    "underscore": "1.8.3"
  }
}
